<!DOCTYPE html>
<html>
  <head>
    <title>Inertia - Search</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "main" %>
    <%= stylesheet_link_tag "normalize" %>
  </head>

  <body>
    <main class="Page Page--flex" data-page="search">
      <!-- Navigation Bar -->
      <nav class="Navbar">
        <div class="Navbar__Search">
          <a class="ImageLink" href="/"><img class="Navbar__Logo" src="/assets/logo.svg"></a>
          <form action="/search">
            <div class="Input__Group Input__Group--Search">
              <input
                class="Input__Default Input--Search"
                placeholder="Search..."
                type="text"
                autocomplete="off"
                autocorrect="off"
                value="<%= params[:query] %>"
                name="query"
              >
              <button for="Search+Submit" class="Button Button--Search">
                <%= inline_svg("icons/search.svg", class: "Icon Icon--on-foreground") %>
              </button>
              <input id="Search+Submit" class="Hidden" type="submit">
            </div>
          </form>
        </div>
        <div class="Navbar__Subbar"></div>
      </nav>
      <!-- End Navigation Bar -->
      <%= yield %>
    </main>
  </body>
</html>
